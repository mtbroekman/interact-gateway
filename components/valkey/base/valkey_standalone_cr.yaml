---
# Source: valkey/templates/valkey-standalone.yaml
apiVersion: valkey.datapower.ibm.com/v1beta2
kind: Valkey
metadata:
  name:  valkey
  namespace: idig
  labels:
    app.kubernetes.io/name: valkey
    helm.sh/chart: valkey-1.0.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/instance: valkey
    app.kubernetes.io/version: 1.0.0
    app.kubernetes.io/component: middleware
  annotations:
    
spec:
  # podSecurityContext is intentionally omitted.
  # On OCP, setting fsGroup: 1000 requires anyuid SCC — unnecessary since the
  # valkey operator works without it. On IKS/AKS fsGroup: 1000 is fine but also
  # not required. If a specific fsGroup is needed for a deployment, patch it in
  # the relevant env overlay.
  kubernetesConfig:
    image: "icr.io/idig/valkey:8.1.4"
    imagePullPolicy: "Always"
    imagePullSecrets: 
    - name: ibm-entitlement-key
    valkeySecret:
      name: "valkey-secret"
      key: "password"
    persistentVolumeClaimRetentionPolicy: 
      whenDeleted: Delete
      whenScaled: Retain
  TLS:
    ca: "ca.crt"
    cert: "tls.crt"
    key: "tls.key"
    secret:
      secretName: "valkey-tls"
  storage:
    volumeClaimTemplate:
      spec:
        storageClassName: PATCHED_BY_KUSTOMIZE
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi

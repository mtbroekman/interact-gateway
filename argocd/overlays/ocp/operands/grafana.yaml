apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-ocp
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "200"
spec:
  destination:
    namespace: grafana
    server: https://kubernetes.default.svc
  project: default
  source:
    path: envs/odf/nonprod/grafana
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    automated:
      prune: true
      selfHeal: true

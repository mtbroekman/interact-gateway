apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: idig-operator
  namespace: argocd
  labels:
    group: applications
  annotations:
    argocd.argoproj.io/sync-wave: "100"
spec:
  destination:
    namespace: idig
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ./components/idig/operator
  syncPolicy:
    syncOptions:
      - ServerSideApply=true
    automated:
      prune: true
      selfHeal: true